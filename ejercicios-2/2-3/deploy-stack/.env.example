# Copy to .env and run: source .env

# Required (for reproducible runs)
export AWS_PROFILE=data-engineer
export AWS_REGION=us-east-1
export STACK_NAME=tecgurus-ec2-demo

# Optional: used only by `plan`
# export CHANGE_SET_NAME=cs-manual-name
