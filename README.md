# Environment installation

    python -m venv .venv
    source .venv/bin/activate
    python -m pip install --upgrade pip
    python -m pip install -r ./requirements.txt

# Others
## Libraries for testing
    python -m pip install pytest pytest-cov vulture moto
